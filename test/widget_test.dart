// Smoke test placeholder — will be replaced by Task 10 home screen tests.
// The counter scaffold has been removed; a full widget test requires a running
// ObjectBox Store which is unavailable in the macOS unit-test sandbox.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
