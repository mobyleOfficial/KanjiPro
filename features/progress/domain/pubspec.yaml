name: progress_domain
description: Domain layer for the progress feature (models, repository contracts, use cases).
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.11.4

dependencies:
  core:
    path: ../../../core
  kanji_domain:
    path: ../../kanji/domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0
