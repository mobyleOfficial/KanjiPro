name: quiz_ui
description: Quiz screen UI module for KanjiPro (mode select, multiple choice, results).
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.0.0
  get_it: ^8.0.0
  common:
    path: ../common
  core:
    path: ../../core
  kanji:
    path: ../../features/kanji
  kanji_domain:
    path: ../../features/kanji/domain
  progress:
    path: ../../features/progress
  progress_domain:
    path: ../../features/progress/domain
  quiz:
    path: ../../features/quiz
  quiz_domain:
    path: ../../features/quiz/domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^10.0.0
  mocktail: ^1.0.0
