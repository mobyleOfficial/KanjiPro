name: kanji_data
description: Data layer for the kanji feature (local JSON data source, model, repository).
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../../../core
  kanji_domain:
    path: ../domain
  json_annotation: ^4.9.0
  injectable: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  json_serializable: ^6.8.0
  mocktail: ^1.0.0
