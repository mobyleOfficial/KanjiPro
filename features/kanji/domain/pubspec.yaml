name: kanji_domain
description: Domain layer for the kanji feature (models, repository contracts, use cases).
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.11.4

dependencies:
  core:
    path: ../../../core

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0
